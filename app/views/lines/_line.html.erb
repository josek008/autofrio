<tr>
	<td><%= line.brand.name %></td>
	<td><%= line.name %></td>
	<td><%= line.products.count %></td>
	<td><%= image_tag line.photo.url(:thumb) %></td>
	<td>
		<div class="btn-group">
			<%= link_to 'Editar', edit_line_path(line), class: 'btn' %>
			<%= link_to 'Eliminar', line, method: :delete,
			data: {confirm: "Existen "+ pluralize(line.products.count, "producto")+ " relacionados con esta linea. Recomendamos que mueva todos los productos a otra linea, antes de eliminar esta. ¿Estás seguro de eliminarla?"}, class: 'btn' %></td>
		</div>
	</td>
</tr>