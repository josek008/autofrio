<div class="form-group">
	<%= f.label "Nombre", class: "col-md-2 control-label" %>
	<div class="col-md-10">
		<%= f.text_field :name, class: "form-control", placeholder: "Incluye el nombre de la marca", required: true %>
	</div>
</div>

<div class="form-group">
	<%= f.label "Imagen", class: "col-md-2 control-label" %>
	<div class="col-md-10">
		<% if b.photo.exists? %>
			<%= image_tag b.photo.url(:medium) %>
		<% end %>
		<%= f.file_field :photo %>
	</div>
</div>

<div class="form-group">
	<div class="col-md-offset-2 col-md-10">
		<%= f.submit "Confirmar", class: "btn btn-lg btn-primary" %>
	</div>
</div>

