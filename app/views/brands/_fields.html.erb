<div class="control-group">
	<%= f.label "Nombre", class: "control-label" %>
	<%= f.text_field :name, placeholder:"Incluye el nombre de la marca", required: true %>
	<% if b.photo.exists? %>
	<%= image_tag b.photo.url(:medium) %>
	<% end %>
	<%= f.file_field :photo %>
</div>